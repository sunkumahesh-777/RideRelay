{
  "name": "riderelay-backend",
  "version": "1.0.0",
  "private": true,
  "description": "RideRelay API and PostgreSQL persistence layer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "db:migrate": "node scripts/migrate.js",
    "db:project": "node scripts/project-db.js",
    "db:status": "node scripts/db-status.js",
    "db:verify": "node scripts/verify-db.js",
    "db:supabase:deploy": "node scripts/deploy-supabase.js",
    "check": "node --check server.js && node --check middleware/auth.js && node --check services/rideService.js && node --check controllers/userController.js && node --check controllers/rideController.js && node --check routes/userRoutes.js && node --check routes/rideRoutes.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "pg": "^8.16.3"
  }
}
